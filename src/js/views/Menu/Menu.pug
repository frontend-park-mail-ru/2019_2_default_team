style
	include Menu.css
mixin item-right(title, href, section)
	li.menu-el-right
		a.inner-menu-element(href=href data-section=section)= title
mixin item-left(title, href, section)
	li.menu-el-left
		a.inner-menu-element(href=href data-section=section)= title


ul.menu
	+item-left('Афиша', '/', 'poster')
	+item-left('О нас', '/about', 'about')
	//- if authorized
	if Auth
		+item-right('Выход', '/logout', 'logout')
		+item-right('Профиль', '/profile', 'profile')
	//- unless authorized
	else
		+item-right('Регистрация', '/register', 'register')
		+item-right('Вход', '/login', 'login')
